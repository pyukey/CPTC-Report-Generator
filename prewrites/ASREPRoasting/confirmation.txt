<div>After compromising the Windows DC, \team used a Sliver extension called sharp-hound-3.</div><div><br></div><div>This can be installed by running <code>armory install sharp-hound-3</code> and then by running <code>sharp-hound-3 All</code> to indicate all collection methods. The files from this tool can then be uploaded to Bloodhound, which allows for querying kerberoastable accounts.</div>