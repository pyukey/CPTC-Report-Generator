\team first ran the following command to identify SUID binaries: <code>find / -perm -4000 -type f 2&gt;/dev/null | grep systemctl</code><br><br>Then, the following command was run to confirm that the <code>systemctl</code> binary had the SUID bit set: <code>ls -l /bin/systemctl | grep 's'</code><br><br><div>As shown in the Figure below, the output confirmed that <code>systemctl</code> had the SUID bit set.</div><div>[Insert image]</div><div>Finally, \team confirmed root privileges with the following command: <code>hostname; whoami; id</code></div>