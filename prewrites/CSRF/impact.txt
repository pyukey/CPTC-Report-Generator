An attacker can host a malicious website containing JavaScript code. If an authenticated user of [Application Name] visits this malicious site, the script can silently send authenticated requests to the application in the background. Because the browser will attach the victim's session cookies and the insecure CORS policy allows the request, the application will process it as a legitimate action. This could allow an attacker to perform unauthorized actions on behalf of the victim, such as [Example Action: modifying account data, transferring funds, or deleting records].