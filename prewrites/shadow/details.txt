\team found that the /etc/shadow file, which stores password hashes, had write permissions for users other than root. This is a major misconfiguration that could allow a user to modify the file, changing the root password.<br><br>If the root password was changed, the desired root may be unable to log in with root privileges, and undesired users instead may be able to log in with root privileges.