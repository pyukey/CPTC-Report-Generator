The permissions of the /etc/passwd file should be changed immediately to prevent unwanted users from gaining root privileges.<br><br><div>To do so, the following command shown below can be used:</div><div><pre><code>chmod 644 /etc/passwd</code></pre></div>Following this, it is recommended that the company implement regular reviews of system file permissions, especially with important files like the /etc/passwd file.<br>